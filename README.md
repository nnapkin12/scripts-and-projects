most js,
kirka.io main scripts im, developing right now

1. ShowOnlyKnife: Kirka.io Texture swapper, Knife only mode, Wireframe, Gunscale script(skins.json is the DB for this script.)

(1) Originally made a hide all weapons and show only knife VM script, to replicate a krunker-like setting, the only reliable way i could find, is whitelisting a knives texture .webp, and blocking everything else in the VM. than realized i could use this logic to upload .webp files and block original texture files, so i made it a texture swap. also added: Wireframe, and Weapon Gun scale mod (x,y,z position, size). I added only rare gun/melee textures to the swapper dropdowns, but you can add any skin texture as a "swappable" skin from the skins.json if you would like, and saves locally. texture swaps update live, so does everything else. started this script from: "https://kirkacommunityhub.pages.dev/assets#scripts#gun-scale-modifier-%2B-wireframe-toggle-%2B-rgb-toggle" 'imnotkoolkid'


2. KirkaTextSwap.js

(2.) WIP texture swapper 
